def apply_constraints(schedule_items, appointments, day_start, day_end):
    """
    MVP: return schedule as-is.
    Later:
    - remove/shift tasks that conflict with appointments
    - enforce day_start/day_end bounds
    - prevent overlaps
    """
    return schedule_items
